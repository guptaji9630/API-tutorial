#include <stdio.h>

int main(){
    int t;
     int arr[i];
    scanf("%d",t);
    int x=0;
    while(x < t){
        int n,s;
        scanf("%d",n);
        scanf("%d",s);
        int arr[n];
        int sum=0;

        for(int i=0;i<n;i++){
            scanf("%d" i);
            sum+=i;
        }
        
        int ans=sum%s;
        x++;
        printf(ans);
        

        
    }

    return 0;
}